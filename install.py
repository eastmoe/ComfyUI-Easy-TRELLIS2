"""Compatibility installer for ComfyUI managers.

Dependencies are declared in requirements.txt and installed by the manager or
by running `pip install -r requirements.txt` from this directory.
"""

if __name__ == "__main__":
    print("No custom install step is required. Install requirements.txt instead.")
