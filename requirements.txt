# Dependencies already provided by ComfyUI requirements.txt.
# Reuse ComfyUI's pinned/base versions; keep these commented to avoid duplicate installs.
# comfy-aimdo
# av
# safetensors
# psutil
# einops
# transformers  # ComfyUI-provided; TRELLIS2 no longer uses it directly after removing rmbg/BiRefNet
# kornia
# pillow>=9.0.0
# numpy>=1.21.0
# tqdm

# comfy-3d-viewers>=0.2.44  # optional: only needed for external 3D preview nodes/workflows
# plyfile      # optional mesh format support; install locally if your trimesh workflow needs it; not import directly
# zstandard    # optional compressed-asset support; not import directly

# Optional accelerated attention backends. TRELLIS2 now falls back to
# ComfyUI attention / PyTorch SDPA when these are not installed.
# flash-attn
# sageattention

# huggingface_hub  # no longer used directly after removing rmbg/BiRefNet
# hf_transfer  # local-only model loading; HF downloads are disabled
# hf_xet       # local-only model loading; HF downloads are disabled
# timm  # removed rmbg/BiRefNet background-removal node; use external SAM/mask nodes for segmentation


# imageio>=2.0.0  # replaced by OpenCV video writer
# easydict         # replaced by local sampler helper

# Optional Triton acceleration. TRELLIS2 uses native PyTorch fallbacks when
# Triton is not installed in the current environment.
# triton>=3.0.0; platform_system == "Linux"
# triton-windows>=3.5.0; platform_system == "Windows"


# Vendored in ./comfy_sparse_attn to avoid PyPI install pressure.
# comfy-sparse-attn==0.1.3
flex_gemm_ap
cumesh_vb
o_voxel_vb_ap
drtk
trimesh
opencv-python-headless  # required by video export and PBR texture inpainting; opencv-python also works
